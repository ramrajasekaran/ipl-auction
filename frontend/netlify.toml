[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Note: API redirects are handled by _redirects file
# which supports environment variable substitution
