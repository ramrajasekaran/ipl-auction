# Proxy API requests to backend
# BACKEND_URL must be set in Netlify Environment Variables
/api/*  :BACKEND_URL/api/:splat  200

# SPA catch-all - serve index.html for all routes (must be LAST)
/*  /index.html  200